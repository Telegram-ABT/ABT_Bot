[tool.poetry]
name = "scaner"
version = "0.1.0"
description = ""
authors = ["Igor Iakovlev <iu.yakovlev@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
telethon = "^1.34.0"
python-dotenv = "^1.0.1"  # Если используется для загрузки переменных окружения
telebot = "^0.0.5"
pymongo = "^4.10.1"
psutil = "^6.1.0"
openai = "^1.52.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
